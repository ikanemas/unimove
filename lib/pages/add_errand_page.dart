import 'package:flutter/material.dart';

class AddErrandPage extends StatelessWidget {
  const AddErrandPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Post Errand')));
  }
}
